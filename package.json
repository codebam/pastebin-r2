{
  "name": "pastebin-ts-worker",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "build": "tsc --project tsconfig.json; node esbuild.mjs",
    "deploy": "wrangler deploy",
    "dev": "wrangler dev",
    "start": "wrangler dev"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240512.0",
    "esbuild-plugin-inline-import": "^1.0.4",
    "typescript": "^5.4.5",
    "wrangler": "^3.57.1"
  },
  "dependencies": {
    "hono": "^4.5.8"
  }
}
